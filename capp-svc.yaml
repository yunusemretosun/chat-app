apiVersion: v1
kind: Service
metadata:
  labels:
    app: chat-app
  name: chat-app
  namespace: capp
spec:
  selector:
    app: chat-app
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 320000